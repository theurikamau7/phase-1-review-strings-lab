// Write your code in this file!

let currentUser = 'Ian'

const firstInitial = currentUser[0];
const restOfName = currentUser.slice(1);

let welcomeMessage = 'Welcome to Flatbook, ' + currentUser + '!'

currentUser = welcomeMessage

const upperCaseCurrentUser = currentUser.toUpperCase();

let excitedWelcomeMessage = upperCaseCurrentUser



excitedWelcomeMessage = upperCaseCurrentUser + '!'

let shortGreeting = `Welcome, ${firstInitial} ${restOfName} !`